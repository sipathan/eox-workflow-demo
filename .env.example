# Copy to `.env` and adjust. Prisma reads this for SQLite and migrations.
# Relative to prisma/ (where schema.prisma lives) → prisma/dev.db at repo root
DATABASE_URL="file:./dev.db"

# Used by the app for session signing (minimum 32 characters).
SESSION_SECRET="local-demo-session-secret-32chars-minimum-ok"
